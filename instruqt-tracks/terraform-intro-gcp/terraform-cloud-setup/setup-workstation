#!/bin/bash -l

set -e

set-workdir /root/hashicat-gcp

cd /root/hashicat-gcp

cp do_not_use/remote_backend.tf remote_backend.tf

cat <<-EOF > /root/.terraform.d/credentials.tfrc.json
{
  "credentials": {
    "app.terraform.io": {
      "token": "YOURTOKEN"
    }
  }
}
EOF

exit 0
